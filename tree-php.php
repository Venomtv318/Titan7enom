<?php 
// echo $_GET; 
// $user = $_POST['data_user'];
// $password = $_POST['data_password'];
// echo "User: $user . password: $password";
// $namecookie = "Ali";
// $cookie_value = "c-cookie";
//     setcookie($namecookie , $cookie_value , time() + (1000 * 100),"/");
//      if(!isset($_COOKIE[$namecookie])) {
//         echo "welcom $cookie_value";
//     }else {
//         echo "you are back! . $_COOKIE[$namecookie]";
//     }
//     session_start();

//     $_SESSION["firstuser"] = "yes is seesion";
//     echo $_SESSION["firstuser"];

//     $_SESSION["lastuser"] = $_GET["user"];
//     echo $_SESSION["lastuser"];
?> 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
</body>
</html>
<?php
// dropshoping 2. Choosing Niches and Products video 3...
/*===========Tree code php===========*/
/*======required to be taught======
array_keys , return , strtolower , format , filter_var , 
FILTER_VALIDATE_EMAIL , mail() , method() , do and while , date_default_timezone_get , void
===========tree code==========
=> include_once() : Ensures that only one specific page is pulled 
=> require : If an error occurs, the script does not continue
=> include : In the event of an error the program will continue
=> declare(strict_types=1); : Aggravated match finder mode
=> define : ID without style variables
=> empty : how word empty 
=>  round() : للتقريب.
=>  abc()   : للمقارنه 
=> is_null  : هل فارغ ام لا.
=> unset()  : للحذف
=> sort() - sort arrays in ascending order
=> rsort() - sort arrays in descending order
=> asort() - sort associative arrays in ascending order, according to the value
=> ksort() - sort associative arrays in ascending order, according to the key
=> arsort() - sort associative arrays in descending order, according to the value
=> krsort() - sort associative arrays in descending order, according to the key
=> scandir - list all files in a directory
=> array_diff - removie dot file
=> mkdir -  creat file
=> copy - copy file 
=> die() - The die() function is an alias of the exit() function
=>
==========operators==========
=> == : Equal bool 
=> === : Equal value and bool
=> != . <> : Are they Equal?
=> echo ($x <=> $y);//قم بإرجاع -1 إذا كانت القيمة الموجودة على اليمين أكبر و ان كان العكس يرجع الينا ب -1
=>  either > or , || , xor
=> both > and , && 
=> \t\ tibe shift
*/
/*===========Tree code SQL and php  =>> video PDO 223 \/\. dropshoping 2. Choosing Niches and Products video 3... 
=> SELECT : Select the full table
=> INSERT INTO : Add data and condition that the columns and data are the same number
=> LIMIT : Determine the number of fields
=> WHERE : Access to a specific part of the table
=> SELECT * FROM `help_category` WHERE `name` LIKE "a%" : Extract data by letter, word or number 
=> SELECT * FROM `help_category` ORDER  BY `id_person` : order by numbering or letter
=> SELECT * FROM student WHERE ID <= 100 LIMIT 2;

INSERT is a keyword to INSERT the data in the table
SQL SYNTAX:
INSERT INTO table_name VALUES (value1, value2, value3);

SQL QUERY:
INSERT INTO fees VALUES(10, 86);

SELECT:
SELECT is a keyword to select the data from the tables.

SQL SYNTAX:

SELECT * FROM table_name;

SQL QUERY:

SELECT * FROM student;

Clause – WHERE, LIMIT
WHERE and LIMIT are called as Clause which are used along with SQL statement to apply the condition.

SQL SYNTAX:

SELECT * FROM table_name WHERE ID <= 100;

SQL QUERY:

SELECT * FROM student WHERE ID <= 100 LIMIT 2;

This query tell the to MySQL to fetch student record where ID field value is less than 100 and fetch only two rows.

Operators – IS NULL, LIKE, ORDER BY
IS NULL, LIKE and ORDER BY are called as operators that you can apply on the condition to check and sort the records.

IS NULL will check if the field is NULL or NOT.

ORDER BY will order / sort the records based on the field.

LIKE has two wild characters:

% – The percent sign represents zero, one, or multiple characters

_ – The underscore represents a single character

SQL SYNTAX:

SELECT * FROM table_name WHERE columnN IS NOT NULL

SELECT * FROM table_name WHERE columnN LIKE pattern;

SELECT * FROM table_name WHERE columnN LIKE pattern ORDER BY columnN;

SQL QUERY:

SELECT * FROM student WHERE student.NAME IS NOT NULL;

This query tell the to MySQL to fetch student record where NAME is not null.

SELECT * FROM student WHERE student.NAME LIKE ‘J%’;

This query tell the to MySQL to fetch student record where NAME matches with J.

SELECT * FROM student WHERE student.NAME LIKE ‘J%’ ORDER BY student.NAME;

This query tell the to MySQL to fetch student record where NAME matches with J and sort the records by NAME.

UPDATE:
UPDATE statement is used to update an existing row in the table based on a condition specified with WHERE clause.

SQL SYNTAX:

UPDATE table_name SET column1 = value1, column2 = value2
WHERE condition;

SQL QUERY:

UPDATE student
SET student.name = ‘Julie’ WHERE ID = 100;



DELETE
DELETE statement is used to delete an existing row in the table based on a condition specified with WHERE clause.

SQL SYNTAX:

DELETE from table_name WHERE condition;

SQL QUERY:

DELETE FROM student WHERE student.name=’John’;
=>
=>
=>
=>
=>
=>
=>
=>
=>
===========*/
/*===========Tree code php===========*/
/*===========Tree code php===========*/
/*===========Tree code php===========*/


    









?>
